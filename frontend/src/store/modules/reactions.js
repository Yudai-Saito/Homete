const state = {
    reactions:["👍", "👀", "💯", "🥰", "🎉"],
}

const getters = {
    reactions: state => state.reactions,
}

const actions = {
}

const mutations = {
}

export default {
    state,
    getters,
    actions,
    mutations
}
